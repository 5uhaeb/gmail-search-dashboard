# ---- Google OAuth ----
# Create an OAuth 2.0 Client ID at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# ---- NextAuth ----
# Generate a strong secret:  openssl rand -base64 32
NEXTAUTH_SECRET=replace-with-a-long-random-string
NEXTAUTH_URL=http://localhost:3000

# ---- Database ----
# Local dev can still use SQLite if you switch prisma/schema.prisma to sqlite:
#   DATABASE_URL="file:./dev.db"
# Production (Vercel + Neon):
DATABASE_URL="postgresql://user:pass@host-pooler.neon.tech/neondb?sslmode=require"

# ---- Optional: restrict the app to your own Google account ----
# If set, only this email address will be allowed to sign in.
# Leave blank to allow any Google account (for personal local use).
ALLOWED_EMAIL=
